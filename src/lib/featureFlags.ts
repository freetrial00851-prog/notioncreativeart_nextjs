/**
 * Sitewide feature toggles — flip to re-enable without code changes elsewhere.
 */

/** Floating "Help" support chat launcher (edge functions stay deployed). */
export const ENABLE_SUPPORT_CHAT = false
